function max(a, b) {
    return (a > b) ? a: b;
}
let maxNumber = max(4, 3);
console.log(maxNumber);


function min(num1, num2) {
    return (num1 < num2) ? num1: num2;
}

let minNumber = min(54, 53);
console.log(minNumber);